# Environment variables template for Health & Speed Checker

# Logging level (debug, info, warn, error)
RUST_LOG=info

# Tauri development mode
TAURI_DEBUG=false

# Database path (optional, defaults to ~/.healthchecker/)
# DATABASE_PATH=/path/to/custom/location

# API endpoints (if telemetry is enabled)
# TELEMETRY_ENDPOINT=https://api.example.com/telemetry
# UPDATE_CHECK_ENDPOINT=https://api.example.com/updates

# Development settings
# DEV_MODE=false
# SKIP_RESTORE_POINTS=false  # WARNING: Only for testing!

# Feature flags (optional)
# ENABLE_EXPERIMENTAL_FEATURES=false
# ENABLE_ADVANCED_CHECKERS=false

# CI/CD settings (for build pipeline)
# GITHUB_TOKEN=your_token_here
# SIGNING_CERTIFICATE_PATH=/path/to/cert
